# Dados locais gerados

O laboratório cria aqui o banco `dabdbt.duckdb` durante a execução. Arquivos DuckDB são ignorados pelo Git e nunca devem ser publicados.

As pequenas amostras CSV versionadas ficam em `../dabdbt/src/seeds/`.
