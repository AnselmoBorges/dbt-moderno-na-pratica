# 05 — Contratos e versões

Execute `python scripts/run_checkpoint.py 05`. O script remove `customer_id` apenas numa cópia temporária e comprova que o contrato da versão 2 bloqueia o build. A árvore de trabalho não é alterada.
