# 03 — MetricFlow local

Execute `python scripts/run_checkpoint.py 03`. O aceite exige configuração semântica válida e retorno mensal das métricas `orders`, `gross_revenue`, `delivered_revenue` e `average_order_value`.
