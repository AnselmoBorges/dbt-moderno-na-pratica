# 04 — Semântica aberta e interoperável

Execute `python course.py checkpoint 04`. O checkpoint consulta as mesmas métricas com MetricFlow, valida o `osi_document.json` e reconcilia os valores na interface SQL governada. APIs, cache e integrações hospedadas pertencem à segunda temporada e não são fingidas pelo laboratório.
