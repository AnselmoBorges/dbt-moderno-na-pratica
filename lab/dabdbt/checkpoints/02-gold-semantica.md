# 02 — Gold não é camada semântica

Execute `python scripts/run_checkpoint.py 02`. Inspecione `src/models/gold/order_fulfillment_g.sql` e compare com `src/models/semantic/schema.yml`: a primeira materializa regras; a segunda dá nomes, entidades, dimensões e agregações governadas.
