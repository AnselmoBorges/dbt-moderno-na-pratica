# 01 — O conteúdo original e o baseline Core 1.12

Execute `python scripts/run_checkpoint.py 01`. O checkpoint carrega seeds, cria a fixture de freshness, executa parse/build e valida `manifest.json`, `semantic_manifest.json`, `osi_document.json` e `run_results.json`.

Core 2.0/Fusion não faz parte do aceite. Consulte o quadro Radar do roteiro e a matriz oficial de recursos para acompanhar a evolução sem instalar software beta.
